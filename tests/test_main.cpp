/**
* @file main.cpp
 * @brief Точка входа для запуска всех тестов Catch2.
 *
 * Макрос `CATCH_CONFIG_MAIN` генерирует функцию `main()` автоматически.
 * Это необходимо, чтобы Catch2 мог собрать и выполнить все тест-кейсы,
 * определённые в других файлах.
 */

#define CATCH_CONFIG_MAIN
#include <catch2/catch_amalgamated.hpp>
